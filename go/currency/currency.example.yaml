# Example currency node for the main application config.yaml.

currency:
  enabled: true
  schema: currency
  base_currency: USD
  currencies:
    - USD
    - BRL
    - EUR
  api_url: https://open.er-api.com/v6/latest/USD
  update_interval: 1h
